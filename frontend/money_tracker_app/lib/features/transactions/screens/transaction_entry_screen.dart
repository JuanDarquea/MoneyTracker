// Temporary placeholder for Task 8 so login_screen.dart compiles. Task 9
// replaces this file with the real transaction entry form/logic.
import 'package:flutter/material.dart';

class TransactionEntryScreen extends StatelessWidget {
  const TransactionEntryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add transaction')),
      body: const Center(child: Text('Coming soon')),
    );
  }
}
